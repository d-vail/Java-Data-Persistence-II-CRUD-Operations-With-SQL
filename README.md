# Java Backend Data Persistence with SQL, Hibernate - Inserting and Modifying Data

Goals of this module are to:
- Perform CRUD operations on an RDBMS using SQL
- Query an RDBMS using JPA and Hibernate